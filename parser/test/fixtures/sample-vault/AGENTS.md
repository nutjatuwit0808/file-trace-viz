# AGENTS

Entry point for every agent. Start at [[po-pipeline]] and check [[dev-impl]] before coding.

QA gate lives in [[qa-checklist#Gates]]. See also [[notes]] for scratch thoughts.

This link is broken on purpose: [[does-not-exist]].

```md
A wikilink inside a code block must be ignored: [[jira-schema]]
```

Inline code too: `[[jira-schema]]` should not create an edge.
