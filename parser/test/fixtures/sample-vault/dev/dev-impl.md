# Dev Impl

Implementation guide. Follows [[po/po-pipeline]] (full-path link) and the root [[AGENTS]] rules.

Duplicate-basename check: [[notes]] must resolve to the shortest path.

A self-link like [[dev-impl]] must not create an edge.
