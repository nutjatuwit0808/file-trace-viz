{
  "name": "@filetraceviz/platform",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/main.js --bundle --format=esm --outfile=dist/bundle.js --log-level=warning",
    "watch": "npm run build -- --watch",
    "serve": "serve -l 5173 ."
  },
  "dependencies": {
    "d3": "^7.9.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "serve": "^14.0.0"
  }
}
